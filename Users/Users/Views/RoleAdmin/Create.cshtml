@using Users.ViewModels.IdentityViewModels;

@model RoleCreateViewModel
@{
    ViewBag.Title = "Създаване на роля";
}

<h2>@ViewBag.Title</h2>
@Html.ValidationSummary()
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-group">
        @Html.LabelFor(m => m.Name, new { @class = "control-label" })
        @Html.EditorFor(m => m.Name, new { htmlAttribute = new { @class = "form-control" } })
    </div>    
    <div class="form-group">
        @Html.LabelFor(m => m.IsAvailableForAdministrators, new { @class = "control-label" })
        @Html.EditorFor(m => m.IsAvailableForAdministrators, new { htmlAttribute = new { @class = "form-control" } })
    </div>
    <button type="submit" class="btn btn-primary">Създай</button>
    @Html.ActionLink("Отказ", "Index", null, new { @class = "btn btn-default" })
}

